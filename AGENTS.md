# AGENTS 规则

## 工作记录

- 每次完成一轮修改后，都要写一段简短总结
- 总结只记录本次改了什么、为什么改、怎么验证的
- 总结要简洁，便于后续快速回忆上下文

## 修改与验证

- 每次代码修改后，必须进行测试或验证
- 只有在验证成功后，才允许进入提交流程
- 如果验证失败，先继续修复，不能跳过验证直接提交

## Git 流程

- 当前 GitHub 仓库按独立仓库维护，不保留 fork 关系；如需跟进上游，只通过本地 `upstream` remote 手工同步
- 验证成功后再执行 `git add` 和 `git commit`
- 提交完成后再执行 `git push`
- 未经验证的修改不得推送到远端
- 当前仓库的 `upstream` 仅用于 `fetch`/`merge`/`rebase` 上游更新，绝不向 `upstream` 推送任何本地提交
- 执行推送时默认只推 `origin`；若需要显式写远端，必须使用 `git push origin <branch>`
- 当前机器的 GitHub 凭证文件在 `~/gh-token.txt`
- 如果 HTTPS 推送失败，优先使用该 token 完成 `origin` 推送，不要重复假设本机没有凭证

## 执行原则

- 优先做最小必要修改
- 修改时保持现有代码风格一致
- 每次启动项目时，默认同时启动 Cloudflare 隧道，并使用生成的可访问域名进行访问
- 如果发现阻塞项，先说明原因，再继续处理

## 最近记录

- 2026-04-23：完成支付后续阶段 11，在 `src/features/payment/subscription-recurring.ts`、`src/app/api/platform/payment/subscription/contracts/[contractId]/route.ts` 与 `src/features/payment/components/auto-renew-contracts-view.tsx` 补齐下周期生效的套餐变更，同时在 `src/features/payment/refund-service.ts`、`src/app/api/platform/payments/admin/refund/route.ts` 与支付中心详情页接上订阅全额退款、积分回收、账单标记 `refunded` 和协议暂停；原因是阶段 10 已把发单和排障补齐，最后缺的是用户态套餐变更和订阅售后策略，补完后业务侧才算闭环；已通过 `pnpm exec tsc --noEmit --pretty false`、`pnpm test:run src/test/payment/subscription-phase11-plan-change-refund.test.ts --reporter=dot` 与 `pnpm test:run src/test/payment/subscription-admin-phase10.test.ts src/test/payment/subscription-recurring-phase9.test.ts src/test/payment/subscription-wechat-phase6.test.ts src/test/payment/subscription-alipay-phase7.test.ts src/test/payment/admin-payment-phase5-refund.test.ts --reporter=dot` 验证
- 2026-04-23：完成支付后续阶段 10，在 `src/features/payment/recurring-provider-service.ts` 与 `src/features/payment/subscription-recurring.ts` 补上连续扣费查约、查单、失败回写、三次连续失败暂停协议和管理员手工补扣，同时把 `src/features/payment/admin.ts` 与支付中心详情页接上协议、账单、失败原因和原始响应查看，并新增三个管理员排障接口；原因是阶段 9 之后虽然已经能发单，但线上还缺主动排障和失败补偿能力，不能算生产可用；已通过 `pnpm exec tsc --noEmit --pretty false`、`pnpm test:run src/test/payment/subscription-admin-phase10.test.ts --reporter=dot` 与 `pnpm test:run src/test/payment/admin-payment-phase4.test.ts src/test/payment/subscription-wechat-phase6.test.ts src/test/payment/subscription-alipay-phase7.test.ts src/test/payment/subscription-recurring-phase9.test.ts --reporter=dot` 验证
- 2026-04-23：完成支付后续阶段 9，在 `src/features/payment/subscription-recurring.ts` 与 `src/features/payment/recurring-provider-service.ts` 接通连续扣费发单逻辑，新增定时任务入口 `src/app/api/jobs/payment/subscription-recurring/route.ts`，让手工 `/bill` 与定时扫描共用同一条发单链路，并补上账期幂等和失败回写；原因是阶段 8 只拆好了渠道动作层，自动续费仍停留在“只建本地账单”，必须继续补成可真实发起扣款的编排层；已通过 `pnpm exec tsc --noEmit --pretty false`、`pnpm test:run src/test/payment/subscription-recurring-phase9.test.ts --reporter=dot` 与 `pnpm test:run src/test/payment/subscription-wechat-phase6.test.ts src/test/payment/subscription-alipay-phase7.test.ts --reporter=dot` 验证
- 2026-04-23：完成支付后续阶段 8，新增 `src/features/payment/recurring-provider-service.ts` 与 `src/features/payment/recurring-provider-notify.ts`，把连续扣费签约链接生成、渠道解约和四个 webhook 入参解析从 `subscription-recurring.ts` 与路由层拆出统一承接；原因是后续要继续补真实扣款、查约和验签，必须先把渠道动作层和通知解析层拆开，避免业务编排文件继续膨胀；已通过 `pnpm exec tsc --noEmit --pretty false` 与 `pnpm test:run src/test/payment/subscription-provider-phase8.test.ts --reporter=dot` 验证
- 2026-04-23：更新 `docs/payment-wechat-alipay-integration-plan.md`，补充对照 `go-pay/gopay` 后的阶段 8 至 11 开发计划、自动续费生产闭环缺口判断，以及支付宝沙箱与微信 mock/小额真实联调测试方案；原因是当前阶段 7 之后最需要的是明确后续开发边界，避免把 mock 闭环误判成自动续费已可直接上线；已通过 `file -bi docs/payment-wechat-alipay-integration-plan.md` 与 `git diff --check -- docs/payment-wechat-alipay-integration-plan.md` 验证
- 2026-04-23：完成微信支付宝后续阶段 7，新增支付宝代扣签约与账单回调接口 `src/app/api/webhooks/alipay/subscription-contract/route.ts`、`src/app/api/webhooks/alipay/subscription-billing/route.ts`，用户侧新增 `/dashboard/subscription/auto-renew` 统一管理微信连续扣费和支付宝代扣，并在 `src/features/payment/subscription-recurring.ts` 与 `src/app/api/platform/payment/subscription/contracts/[contractId]/route.ts` 补齐查约、续费、解约闭环；原因是文档阶段 7 要求平台能查约、解约、续费、查单，不能只做签约入口；已通过 `pnpm exec tsc --noEmit --pretty false`、`pnpm test:run src/test/payment/subscription-alipay-phase7.test.ts --reporter=dot` 验证
- 2026-04-22：完成微信支付宝后续阶段 6，新增 `subscription_contract / subscription_billing` 表、`src/features/payment/subscription-recurring.ts` 与微信连续扣费相关接口，当前已能通过签约接口、签约回调、账单生成接口和账单回调把微信自动续费订阅落到 `subscription + sales_order + creditsBatch`；原因是自动续费不能继续只依赖一次性 `payment_intent`，必须先补协议层和周期账单层；已通过 `set -a; source .env.test; set +a; psql "$DATABASE_URL" -f drizzle/0017_swift_agreement.sql`、`pnpm exec tsc --noEmit --pretty false` 与 `pnpm test:run src/test/payment/subscription-wechat-phase6.test.ts --reporter=dot` 验证
- 2026-04-22：完成微信支付宝后续阶段 5，新增 `src/features/payment/refund-service.ts` 与 `src/app/api/platform/payments/admin/refund/route.ts`，管理员现在可在 `/admin/payments` 对积分包订单发起退款，退款时会先回收对应积分，再写 `sales_after_sales_event` 并把全额退款的 `payment_intent` 标成 `refunded`；原因是支付中心在查单之后必须尽快具备可闭环的售后入口；已通过 `pnpm exec tsc --noEmit --pretty false` 与 `pnpm test:run src/test/payment/admin-payment-phase5-refund.test.ts --reporter=dot` 验证
- 2026-04-22：完成微信支付宝后续阶段 4，新增 `src/features/payment/admin.ts`、`src/app/api/platform/payments/admin/route.ts`、`src/app/api/platform/payments/admin/[orderId]/route.ts` 与后台页面 `src/app/[locale]/(admin)/admin/payments/page.tsx`，管理员现在可在 `/admin/payments` 查询支付列表并查看 `payment_intent + sales_order + 订单项 + 售后事件` 详情；原因是后续退款和代扣都需要先有统一的后台查单入口；已通过 `pnpm exec tsc --noEmit --pretty false` 与 `pnpm test:run src/test/payment/admin-payment-phase4.test.ts --reporter=dot` 验证
- 2026-04-22：补充 `docs/payment-wechat-alipay-integration-plan.md` 的后续扩展分析，新增微信连续扣费、支付宝代扣订阅、后台退款与查单界面的承接方案，明确当前应把一次性支付层、订阅协议层、周期账单层和售后层拆开推进，并建议后续顺序为“先查单、再退款、后做微信连续扣费、最后做支付宝代扣订阅”；原因是当前仓库已具备 `subscription/sales_order/sales_after_sales_event` 基础，但缺少协议关系与周期扣款模型，不能继续只靠 `payment_intent` 承接自动续费；已通过 `file -bi docs/payment-wechat-alipay-integration-plan.md` 与 `git diff --check -- docs/payment-wechat-alipay-integration-plan.md AGENTS.md` 验证
- 2026-04-22：完成微信支付宝接入阶段 3，把 `.env.example` 补齐 `PAYMENT_MOCK_MODE`、微信支付、支付宝所需环境变量，并新增最终验收测试 `src/test/payment/payment-phase3-acceptance.test.ts`，覆盖“创建支付单 -> 微信成功回调 -> 再读支付状态已 paid”；原因是前两阶段虽然链路已经打通，但还缺直接可照抄的配置入口和最后一条完整验收链；已通过 `pnpm exec tsc --noEmit --pretty false` 与 `pnpm test:run src/test/payment/payment-phase3-acceptance.test.ts --reporter=dot` 验证
- 2026-04-22：完成微信支付宝接入阶段 2，新增 `src/features/payment/provider-notify.ts` 与 `src/features/payment/settlement.ts`，`src/app/api/webhooks/wechat-pay/route.ts`、`src/app/api/webhooks/alipay/route.ts` 已把三方支付成功通知回写到 `payment_intent`，并统一落 `sales_order`、发积分、结算分销；原因是阶段 1 只有下单入口，还不能真正入账；已通过 `pnpm exec tsc --noEmit --pretty false` 与 `pnpm test:run src/test/payment/payment-webhook-phase2.test.ts --reporter=dot` 验证
- 2026-04-22：完成微信支付宝接入阶段 1，新增 `payment_intent` 本地待支付单，`src/features/payment/payment-intents.ts` 与 `src/features/payment/credit-purchase.ts` 已统一生成积分购买支付单，`src/features/payment/provider-service.ts` 支持 `creem/wechat_pay/alipay` 下单参数生成；新增接口 `src/app/api/platform/payment/credit-purchase/route.ts`、`src/app/api/platform/payment/intents/[intentId]/route.ts`，并把 `src/app/[locale]/(dashboard)/dashboard/credits/buy/buy-credits-view.tsx` 改成先选支付方式再进入站内收银台页；原因是当前项目缺少本地待支付单，无法稳妥承接微信支付宝；已通过 `psql "$DATABASE_URL" -f drizzle/0016_quick_payment_intent.sql`、`pnpm exec tsc --noEmit --pretty false` 与 `pnpm test:run src/test/payment/credit-purchase-phase1.test.ts --reporter=dot` 验证
- 2026-04-19：修复 RedInk 完整模板启用后的花括号转义问题，`src/features/tool-config/tool-definitions.ts` 现把 `text1~text3` 的 JSON 示例改成双花括号，`src/features/tool-config/service.ts` 增加对旧版未转义默认模板的识别回写；同时用 `psql` 把 `nextdevtpl` 项目当前 `redink.text1~text3` 的 `tool_config_field.default_value_json` 与 `tool_config_value(project_admin)` 一并替换成转义版模板；原因是完整模板启用后，单花括号 JSON 示例会被 `format_map` 误当成格式字段并报 `Invalid format specifier`；已通过 `pnpm exec biome check src/features/tool-config/tool-definitions.ts src/features/tool-config/service.ts`、`pnpm test:run src/test/tool-config/service.test.ts src/test/platform/redink-user-settings-phaseD.test.ts --reporter=dot`，以及 `psql` 实查 `text1~text3` 已包含 `{{` / `}}` 验证
- 2026-04-19：把平台 `redink.text1~text4` 改回“完整提示词模板”语义，`src/features/tool-config/tool-definitions.ts` 现直接把商品图片理解、商品文案、商品发布文案、商品发布图四套完整模板写入默认值，字段说明同步改成变量模板说明；`src/features/tool-config/service.ts` 取消仅默认项目才拿到 RedInk 模板的分支，并在重新 seed 时把空白的 `project_admin` 模板值回写为完整模板，避免历史空值继续覆盖；原因是当前链路要求提示词所见即所得，不能再把 `text1~text4` 当补充语句；已通过 `pnpm exec biome check src/features/tool-config/tool-definitions.ts src/features/tool-config/service.ts src/test/platform/redink-user-settings-phaseD.test.ts src/test/tool-config/service.test.ts`、`pnpm test:run src/test/tool-config/service.test.ts src/test/platform/redink-user-settings-phaseA.test.ts src/test/platform/redink-user-settings-phaseD.test.ts --reporter=dot`，以及 `psql postgresql://postgres:123456@127.0.0.1:5432/nextdevtpl -c "SELECT ... redink text1~text4 ..."` 实查 `nextdevtpl` 当前字段默认值和项目管理员值均为完整模板验证
- 2026-04-19：把后台 `/admin/storage` 最近资源明细里的“预览”从外链打开改成站内弹窗预览，`src/features/storage/components/admin-storage-view.tsx` 现按资源 `contentType` 在 `Dialog` 内直接展示图片、视频、音频、PDF 和文本，其他类型仍保留下载链接；原因是资源明细页的“预览”不该跳转下载，管理员需要在当前页直接核对媒体内容；已通过 `pnpm exec biome check src/features/storage/components/admin-storage-view.tsx src/test/storage/admin-storage-view.test.tsx` 与 `pnpm test:run src/test/storage/admin-storage-view.test.tsx --reporter=dot` 验证
- 2026-04-19：优化对象存储复用与明细展示，给 `src/features/storage/records.ts` 新增 `findReusableStorageObject/confirmStorageObjectUpload`，上传接口 `src/app/api/platform/storage/presigned-image/route.ts` 与 `src/app/api/upload/presigned/route.ts` 支持可选 `checksumSha256` 命中同用户同文件后直接复用并返回 `reused=true`，同时把 `fileSize/source/sha256` 写入元数据；在 `src/app/api/platform/storage/object/route.ts` 增加上传完成回写 `POST`，并让 `src/app/api/platform/storage/local-upload/route.ts` 在本地上传成功后自动回写 `ready+size`，修复大量 `pending/0B`；后台页 `src/features/storage/components/admin-storage-view.tsx` 与 `src/app/[locale]/(admin)/admin/storage/page.tsx` 增加最近资源分页、来源字段、CDN 优先预览/下载链接，`records.ts` 同步返回 `cdnBaseUrl/recentPagination/links`；原因是当前资源明细可用性不足，难以排查和复用同文件上传；已通过 `pnpm exec biome check src/features/storage/records.ts src/app/api/platform/storage/presigned-image/route.ts src/app/api/upload/presigned/route.ts src/app/api/platform/storage/object/route.ts src/app/api/platform/storage/local-upload/route.ts src/features/storage/components/admin-storage-view.tsx 'src/app/[locale]/(admin)/admin/storage/page.tsx' src/test/platform/storage-phase2-lifecycle.test.ts src/test/storage/admin-storage-view.test.tsx`、`pnpm test:run src/test/platform/storage-phase2-lifecycle.test.ts src/test/storage/admin-storage-view.test.tsx --reporter=dot` 与 `pnpm exec tsc --noEmit --pretty false` 验证
- 2026-04-19：完成 RedInk 配置分层改造阶段 E，把 `src/features/tool-config/tool-definitions.ts` 里的 `redink.text1~text4` 默认值改成空白补充提示词，并在字段说明里明确“只填写自然语言补充，不需要变量或 JSON 结构”；同时让 `seedDefaultToolConfigProject()` 在内置字段回写时同步恢复 `enabled/defaultValueJson/optionsJson/validationJson`，修复历史库里提示词字段被停用后界面不显示的问题，并新增 `src/test/platform/redink-user-settings-phaseD.test.ts` 覆盖用户侧说明文案，`src/test/tool-config/service.test.ts` 覆盖重新 seed 恢复字段启用状态；原因是 RedInk 用户配置不应该再承载整段模板，否则会把 JSON 花括号示例写进数据库并触发运行时格式化报错；已通过 `pnpm exec biome check src/features/tool-config/service.ts src/features/tool-config/tool-definitions.ts src/test/tool-config/service.test.ts src/test/platform/redink-user-settings-phaseD.test.ts` 与 `pnpm test:run src/test/tool-config/service.test.ts src/test/platform/redink-user-settings-phaseA.test.ts src/test/platform/redink-user-settings-phaseD.test.ts --reporter=dot` 验证
- 2026-04-18：完成 RedInk 配置分层改造阶段 C 联调回归确认，阶段 A/B/C 组合后用户侧仍只暴露 `text1~text4`，且平台默认提示词模板没有影响用户态字段过滤；原因是图片提示词现已引用文案提示词和生成文案，需要确认平台端阶段 A 的权限边界没有被后续阶段打穿；已通过 `pnpm test:run src/test/platform/redink-user-settings-phaseA.test.ts --reporter=dot` 验证
- 2026-04-18：完成 RedInk 配置分层改造阶段 B，在 `src/features/tool-config/tool-definitions.ts` 给 `redink.text1~text4` 写入平台默认提示词模板，供外部 RedInk 用户设置页直接读取和覆盖；原因是阶段 A 虽然收口了权限边界，但用户提示词还没有默认值，不能直接拿来驱动生成链路；已通过 `pnpm exec biome check src/features/tool-config/tool-definitions.ts` 与 `pnpm test:run src/test/platform/redink-user-settings-phaseA.test.ts --reporter=dot` 验证未回退
- 2026-04-18：完成 RedInk 配置分层改造阶段 A，把 `src/features/tool-config/tool-definitions.ts` 中 `redink` 的非提示词槽位全部改成仅管理员可见可配，用户态只保留 `text1~text4` 四个提示词字段，并新增接口级测试 `src/test/platform/redink-user-settings-phaseA.test.ts` 覆盖“用户只看得到提示词字段”和“用户不能写入管理员字段”；原因是 `redink` 的默认模型、路由策略、首选服务商、白名单与功能规则不应再出现在用户自己的设置里；已通过 `pnpm exec biome check src/features/tool-config/tool-definitions.ts src/test/platform/redink-user-settings-phaseA.test.ts` 与 `pnpm test:run src/test/platform/redink-user-settings-phaseA.test.ts --reporter=dot` 验证
- 2026-04-17：修复 `start-dev.sh` 重启开发服务时只结束监听端口的 `next-server` 子进程、父级 `node/sh/pnpm` 仍持有 `.next/dev/lock`，导致再次执行脚本触发 `Unable to acquire lock`；现改为 Unix 按进程组结束旧实例、Windows 用 `taskkill /T /F` 结束整棵树，并在端口未释放时补一次强制停止；已通过 `bash -n start-dev.sh`、`START_TUNNEL=0 SKIP_INSTALL=1 DETACH=1 ./start-dev.sh` 连续执行两次，确认第二次会先清理旧实例再重启，日志中不再出现 lock 报错验证
- 2026-04-17：把平台级对象存储策略从 `tool-config(storage)` 拆到 `project.metadata.storagePolicy`，新增 `saveStoragePolicyAction/saveStoragePolicyConfig` 供 `/admin/storage` 独立保存，同时在 `/admin/tool-config` 和工具定义状态区隐藏 `storage`，保留 `tool_storage_rule` 只作为工具定义派生出的查询表；原因是 `storage` 属于平台基础设施，不应继续作为业务工具暴露；已通过 `psql postgresql://postgres:123456@127.0.0.1:5432/nextdevtpl -c "ALTER TABLE project ADD COLUMN IF NOT EXISTS metadata json;"`、`pnpm exec biome check src/db/schema.ts src/features/storage/records.ts src/features/storage/actions.ts src/features/storage/components/admin-storage-view.tsx src/features/tool-config/service.ts src/features/tool-config/definition-admin.ts src/test/platform/storage-phase2-lifecycle.test.ts src/test/platform/storage-phase45-policy.test.ts src/test/tool-config/tool-definition-phase1.test.ts src/test/storage/admin-storage-view.test.tsx drizzle/meta/_journal.json`、`pnpm exec tsc --noEmit --pretty false`、`pnpm test:run src/test/storage/admin-storage-view.test.tsx src/test/platform/storage-phase2-lifecycle.test.ts src/test/platform/storage-phase45-policy.test.ts src/test/tool-config/tool-definition-phase1.test.ts --reporter=dot` 与 `git diff --check` 验证
- 2026-04-18：给 `redink` 工具定义补齐用户侧可读槽位名，把 `config1/config2/config3/secret1/json1/json2/json3` 显示成默认模型、路由策略、首选服务商等名称，避免外部 RedInk 设置页继续暴露内部槽位名；已通过 `pnpm test:run src/test/tool-config/api.test.ts src/test/tool-config/tool-definition-phase1.test.ts --reporter=dot` 验证
- 2026-04-17：把公开接口文档页从“固定头部直接压在文档布局上”改成“固定头部 + 文档主体真实下移”的上下布局，`src/app/[locale]/docs/layout.tsx` 现用 `tripai-docs-page + pt-16` 包住 `DocsLayout`，文档专用头部 `z-index` 降到搜索弹窗之下，并在 `src/app/globals.css` 给文档页搜索弹窗补顶部偏移；原因是此前内容区、侧栏搜索按钮和弹出搜索框都与固定头部共用同一页面原点，属于结构冲突，不该继续靠局部偏移硬顶；已通过 `curl http://10.0.160.121:3000/zh/docs/tool-integration-api` 实际确认页面输出包含 `tripai-docs-page`、`pt-16`、文档头部 `z-40` 与文档布局根节点，并通过 `git diff --check -- src/app/[locale]/docs/layout.tsx src/features/marketing/components/header.tsx src/app/globals.css` 验证
- 2026-04-17：把公开接口文档页切到专用头部，给 `src/features/marketing/components/header.tsx` 增加 `variant`，文档布局改用单层 `docs` 头部并把 `src/app/[locale]/docs/layout.tsx` 的 `--fd-header-height` 收到 `4rem`；原因是营销页原先的双层固定导航和底部浮动导航结构不适合文档页，会持续干扰 Fumadocs 侧栏与搜索入口定位；已通过 `curl http://10.0.160.121:3000/zh/docs/tool-integration-api` 实际确认页面输出只剩单层 `h-16` 顶栏、不再包含营销页第二行快捷链接和底部导航，并通过 `git diff --check -- src/features/marketing/components/header.tsx src/app/[locale]/docs/layout.tsx src/app/globals.css` 验证
- 2026-04-17：继续修复公开接口文档页左上角搜索按钮被顶部导航遮挡，给文档布局容器补 `tripai-docs-layout` 标记，并在 `src/app/globals.css` 覆盖 Fumadocs 侧栏占位容器 `data-sidebar-placeholder` 的 `top/height`，让桌面侧栏从 `--fd-docs-row-2` 开始排版；原因是侧栏默认只按 `--fd-docs-row-1` 计算起点，没有把站点固定顶栏算进去，导致大搜索按钮被压在导航下面；已通过 `curl http://10.0.160.121:3000/zh/docs/tool-integration-api` 实际确认页面输出含 `tripai-docs-layout` 与侧栏节点标记，且 `git diff --check -- src/app/[locale]/docs/layout.tsx src/app/globals.css` 验证
- 2026-04-17：修复公开接口文档页顶部遮挡，给 `src/app/[locale]/docs/layout.tsx` 的 `DocsLayout` 补上与站点固定导航一致的 `--fd-header-height`，并把 `src/app/globals.css` 的全局锚点 `scroll-margin-top` 调整为桌面 `6rem`、移动端 `9rem`；原因是文档页复用营销站固定顶栏后，Fumadocs 仍按 `0px` 头部高度布局，导致首屏正文和锚点标题钻到导航下面；已通过 `curl http://10.0.160.121:3000/zh/docs/tool-integration-api` 实际确认页面输出含 `--fd-header-height:5rem` / `max-md:[--fd-header-height:8rem]`，并通过 `git diff --check -- src/app/[locale]/docs/layout.tsx src/app/globals.css` 验证
- 2026-04-17：继续接通公开接口文档页，修复 `src/content/docs/tool-integration-api.mdx` 在 `Tabs` 内直接混写 Markdown 列表导致 MDX 编译失败、`/zh/docs/tool-integration-api` 返回 500 的问题；原因是之前文档路由和内容已接回，但文档正文里有两处 `Tab` 子节点被列表语法打断；已通过 `curl -I http://10.0.160.121:3000/zh/docs/tool-integration-api` 返回 `HTTP/1.1 200 OK`、`curl http://10.0.160.121:3000/zh/docs/tool-integration-api` 实际取回页面 HTML 与 `git diff --check -- src/content/docs/tool-integration-api.mdx src/app/[locale]/docs/[[...slug]]/page.tsx src/app/[locale]/docs/layout.tsx src/content/docs/index.mdx src/features/marketing/components/header.tsx src/app/globals.css` 验证
- 2026-04-17：完成工具插件架构阶段 5，新增 `tool_definition_import_log` 表与 `src/features/tool-config/definition-admin.ts`，落地 `/api/platform/tools`、`/api/platform/tools/import`、`/api/platform/tools/[toolKey]/disable`、`/api/platform/tools/[toolKey]/rollback` 以及后台工具定义导入、状态展示、停用和回滚操作；原因是要把“新增普通工具必须改代码”收口成后台可导入的定义数据流，并保留导入审计和最近状态；已通过 `pnpm exec biome check "src/app/[locale]/(admin)/admin/tool-config/page.tsx" src/features/tool-config/actions.ts src/features/tool-config/components/admin-tool-config-view.tsx src/features/tool-config/definition-admin.ts src/features/tool-config/schema.ts src/app/api/platform/tools/route.ts src/app/api/platform/tools/import/route.ts "src/app/api/platform/tools/[toolKey]/disable/route.ts" "src/app/api/platform/tools/[toolKey]/rollback/route.ts" src/test/tool-config/tool-import-phase5.test.ts src/test/tool-config/admin-tool-config-view.test.tsx`、`pnpm exec tsc --noEmit --pretty false` 与 `pnpm test:run src/test/tool-config/tool-import-phase5.test.ts src/test/tool-config/tool-runtime-phase4.test.ts src/test/tool-config/admin-tool-config-view.test.tsx --reporter=dot` 验证
- 2026-04-17：完成工具插件架构阶段 4，新增 `tool_runtime_token` 与 `tool_launch_ticket` 表并落地 `src/features/tool-config/runtime-auth.ts`，`/api/platform/tool-config/runtime`、`runtime-save`、`revision` 已切到工具级 token 校验，新增 `/api/platform/tools/[toolKey]/launch` 和 `/api/platform/tools/session/exchange` 供外部工具完成启动票据换取用户身份；原因是要把原先全局 `TOOL_CONFIG_RUNTIME_TOKEN` 改成按工具隔离的运行时协议，降低一个工具泄露 token 影响全平台的风险；已通过 `pnpm test:run src/test/tool-config/tool-runtime-phase4.test.ts --reporter=dot`、`pnpm test:run src/test/tool-config/api.test.ts src/test/tool-config/external-runtime-flow.test.ts --reporter=dot`、`pnpm exec tsc --noEmit --pretty false` 与 `psql postgresql://postgres:123456@127.0.0.1:5432/nextdevtpl -c "CREATE TABLE IF NOT EXISTS tool_runtime_token (...); CREATE TABLE IF NOT EXISTS tool_launch_ticket (...);"` 验证
- 2026-04-17：完成工具插件架构阶段 3，新增 `tool_storage_rule` 表并在 `seedDefaultToolConfigProject()` 中按工具定义补齐工具级对象存储规则，`src/features/storage/records.ts` 改为把后台 `storage.json1` 与工具存储规则合并计算生命周期，`src/app/api/platform/storage/presigned-image/route.ts` 已支持显式传 `toolKey + purpose` 选择上传前缀；原因是要把 RedInk 等工具的临时目录从平台默认配置里拆出去，避免后续每加一个工具都去改 `storage` 默认值；已通过 `pnpm test:run src/test/platform/tool-storage-phase3.test.ts --reporter=dot`、`pnpm test:run src/test/platform/storage-phase2-lifecycle.test.ts --reporter=dot`、`pnpm test:run src/test/platform/storage-phase45-policy.test.ts --reporter=dot`、`pnpm exec tsc --noEmit --pretty false` 与 `psql postgresql://postgres:123456@127.0.0.1:5432/nextdevtpl -c "CREATE TABLE IF NOT EXISTS tool_storage_rule (...)"` 验证
- 2026-04-17：完成工具插件架构阶段 2，新增 `tool_feature` 表并在 `seedDefaultToolConfigProject()` 中按工具定义补齐功能清单，`src/features/ai-gateway/service.ts` 改为从 `tool-definitions` 的 `features.pricing` 自动补齐默认 `ai_pricing_rule`，不再写死 RedInk 默认计费规则；原因是要把 AI 计费入口从工具特判收口到统一数据源，给后续导入工具定义和后台管理铺路；已通过 `pnpm test:run src/test/platform/tool-pricing-phase2.test.ts --reporter=dot`、`pnpm test:run src/test/platform/ai-chat-redink-defaults.test.ts --reporter=dot`、`pnpm exec tsc --noEmit --pretty false` 与 `psql postgresql://postgres:123456@127.0.0.1:5432/nextdevtpl -c "CREATE TABLE IF NOT EXISTS tool_feature (...)"` 验证
- 2026-04-17：完成工具插件架构阶段 1，新增 `src/features/tool-config/tool-definitions.ts` 统一维护内置工具入口元数据、槽位标签、默认运行时配置与 RedInk/Jingfang AI 的基础定义，`seedDefaultToolConfigProject()` 改为按工具定义写入 `tool_registry.metadata` 和字段定义，同时补上 `drizzle/0010_smiling_lady_ursula.sql` 与阶段测试 `src/test/tool-config/tool-definition-phase1.test.ts`；原因是先把平台内核中的工具写死点收口到单一数据源，为后续计费、存储和外部协议阶段做准备；已通过 `pnpm test:run src/test/tool-config/tool-definition-phase1.test.ts --reporter=dot`、`pnpm test:run src/test/tool-config/service.test.ts src/test/tool-config/api.test.ts src/test/tool-config/external-runtime-flow.test.ts --reporter=dot`、`pnpm exec tsc --noEmit --pretty false` 与 `psql postgresql://postgres:123456@127.0.0.1:5432/nextdevtpl -c "ALTER TABLE tool_registry ADD COLUMN IF NOT EXISTS metadata json;"` 验证
- 2026-04-17：新增工具可插拔接入架构分析文档，梳理当前工具配置、RedInk、AI 网关、积分、对象存储和认证的耦合点，并给出工具定义数据化、工具级 runtime token、外部工具启动票据和分阶段落地方案；已通过 `file -bi docs/tool-plugin-architecture-analysis.md`、`wc -l docs/tool-plugin-architecture-analysis.md` 与 `git diff --check` 验证
- 2026-04-16：下架公开开发文档和博客入口，移除导航、站点地图和搜索入口中的相关暴露，并把对外品牌文案从 NextDevTpl 改为 tripai；同时新增平台配置项“新用户注册奖励积分”，让管理员可改默认注册积分，并把 Turnstile 验证扩展到邮箱注册；已通过 `git diff --name-only -z | xargs -0 pnpm exec biome check`、`pnpm test:run src/test/auth/sign-in-captcha.test.ts src/test/tool-config/service.test.ts src/test/ai-gateway/admin-ai-gateway-view.test.tsx --reporter=dot`、`pnpm exec tsc --noEmit --pretty false`、`pnpm exec next build`、`curl -I http://127.0.0.1:3312/zh`、`curl -I http://127.0.0.1:3312/zh/docs` 与 `curl -I http://127.0.0.1:3312/zh/blog` 验证
- 2026-04-15：把 NextDevTpl 部署脚本改成项目内配置驱动，`scripts/deploy.sh` 现在只做拉镜像和部署，不再自动创建 PostgreSQL 容器，也不再依赖根目录 `docker-compose.yml`；数据库地址可直接用 `DATABASE_URL`，也可用 `DB_HOST/DB_NAME/DB_USER/DB_PASSWORD/DB_PORT` 组合，默认库名为 `nextdevtpl`，并支持用 `DOCKER_NETWORK` 接入手动准备好的共享网络；已通过 `python3 -m py_compile deploy.py`、临时 PostgreSQL 容器 `tripsass-pg-verify`、`DOCKER_NETWORK=tripsass-verify DATABASE_URL=postgresql://postgres:postgres@tripsass-pg-verify:5432/nextdevtpl_verify python3 deploy.py deploy 20260415 --skip-login --skip-pull --env-file /tmp/nextdevtpl-verify.env`、`curl -I http://127.0.0.1:3307`、`curl http://127.0.0.1:3307/api/platform/session` 与查询 `nextdevtpl_verify` 库 `public` 表数为 36 验证
- 2026-04-15：补上 PostgreSQL 自动建库链路，新增 `scripts/ensure-postgres-database.mjs` 让容器启动先检查并创建 `DATABASE_URL` 指向的业务库，再执行 `drizzle-kit migrate`；同时 `scripts/deploy.sh` 与 `deploy.py` 生成 `.env.release` 时会保留已有额外环境变量，默认数据库密码改为 `postgre4250`；已通过临时 PostgreSQL 实测自动创建 `nextdevtpl_auto` 库并执行 Drizzle 迁移后得到 36 张业务表验证
- 2026-04-15：新增 `scripts/render-nginx.sh`，平台服务现在可直接按 `scripts/release.env` 中的 `DOMAIN/WWW_DOMAIN/APP_*` 生成 Nginx HTTPS 反代配置，统一透传 `X-Forwarded-*` 与 websocket 头；已通过 `bash -n scripts/render-nginx.sh` 与直接渲染配置验证
- 2026-04-15：新建 `review/upstream-20260415` 评估分支，只挑上游设置页“删除账户”修复合入当前 fork：在 `src/features/settings/actions/index.ts` 补删除账户 server action，设置页危险区改成确认弹窗并在成功后退出登录，同时补中英文提示文案与设置测试；原因是上游这组修改在当前分支仍缺失，而 `auth/credits/drizzle` 等其他上游提交已被本地改动覆盖或冲突过高，不适合整包合并；已通过 `pnpm exec biome check src/features/settings/actions/index.ts src/features/settings/components/settings-profile-view.tsx messages/zh.json messages/en.json src/test/settings/profile.test.ts`、`pnpm test:run src/test/settings/profile.test.ts --reporter=dot` 与 `pnpm exec tsc --noEmit --pretty false` 验证
- 2026-04-15：把 NextDevTpl 部署入口补成真正的一键 `release`，`scripts/deploy.sh` 现在会自动构建、推送、拉取并部署，`scripts/release.env` 已写入阿里云镜像仓库密码，部署前会自动停掉占用端口 3000 的本机进程，并在每次部署时同步 `.env.release`；原因是要让平台与 RedInk 在同机上无需手工输入即可重复发布；已通过 `bash scripts/deploy.sh 20260415`、`curl -I http://127.0.0.1:3000`、`curl http://127.0.0.1:3000/api/platform/session` 与 `curl -I https://platform.tripai.icu` 验证
- 2026-04-15：新增 `deploy.py`、`scripts/build.sh`、`scripts/deploy.sh` 与 `scripts/release.env`，让 NextDevTpl 可按阿里云镜像仓库配置一键构建、推送、拉取和部署，并在 `deploy.py deploy` 时自动拉起同网段 PostgreSQL 容器；同时给 Dockerfile 补 `BETTER_AUTH_URL/NEXT_PUBLIC_APP_URL/REDINK_PUBLIC_URL` 构建参数，原因是需要把平台和 RedInk 一起接到统一镜像发布链路；已通过 `python3 -m py_compile deploy.py`、`python3 deploy.py build localtest --skip-login --skip-push`、`python3 deploy.py deploy localtest --skip-login --skip-pull --env-file /tmp/nextdevtpl-localtest.env` 与 `curl -I http://127.0.0.1:3306` 验证
- 2026-04-14：给 `/zh/admin/ai` 的模型绑定页补上当前页签和表单草稿的 `sessionStorage` 恢复，提交成功或取消编辑时清掉草稿；原因是该页原本只用 React 本地状态，开发环境下页面一旦被热更新或重挂载就会把正在填写的模型绑定内容清空；已通过 `pnpm exec biome check src/features/ai-gateway/components/admin-ai-gateway-view.tsx src/test/ai-gateway/admin-ai-gateway-view.test.tsx` 与 `pnpm test:run src/test/ai-gateway/admin-ai-gateway-view.test.tsx --reporter=dot` 验证，`pnpm exec tsc --noEmit --pretty false --incremental false` 仍被当前运行中的 `.next/dev/types/routes.d.ts` 与 `validator.ts` 生成语法错误阻塞
- 2026-04-14：给登录页补上“记住账号”和 Cloudflare Turnstile 机器人验证，前端记住上次登录邮箱并把 `rememberMe` 传给 Better Auth，服务端则直接启用 Better Auth 自带的 Turnstile 插件只拦 `/sign-in/email`；原因是登录页缺少基础易用性和反机器刷接口能力；已通过 `pnpm exec biome check --write src/lib/auth/index.ts src/lib/auth/client.ts src/features/auth/components/sign-in-form.tsx messages/zh.json messages/en.json src/test/auth/sign-in-captcha.test.ts`、`timeout 60s pnpm exec tsc --noEmit --pretty false --incremental false` 与 `timeout 90s pnpm test:run src/test/auth/sign-in-captcha.test.ts --reporter=dot` 验证
- 2026-04-13：清掉 `tool_config_value/tool_config_audit_log` 里的孤儿用户配置，给 `1183989659@qq.com` 手动补发 500 积分到 509 可用余额，并收口 RedInk/AI Chat 回归；原因是共享测试库脏数据、积分余额过低和并行测试里的 provider 能力串扰一起导致 webhook、newsletter、storage、tool-config、redink 目录与 AI Chat 多处误报；代码上把积分不足文案统一成“账户积分不足”、让非默认项目的 RedInk 首次访问保持空目录、把 AI 网关改成先按 provider 限制再判模型能力，并把易串扰测试换成独立模型名；已通过 `pnpm exec biome check --write src/app/api/platform/storage/presigned-image/route.ts src/features/credits/core.ts src/features/mail/actions.ts src/features/ai-gateway/service.ts src/features/tool-config/service.ts src/test/platform/api.test.ts src/test/platform/ai-chat-redink-defaults.test.ts src/test/platform/redink-model-options.test.ts src/test/platform/storage-phase1-provider.test.ts src/test/platform/storage-phase3-cleanup.test.ts src/test/payment/webhook.test.ts src/test/tool-config/user-tool-config-section.test.tsx`、`pnpm exec tsc --noEmit --pretty false` 与 `pnpm test:run` 全量 345 通过 1 跳过验证
- 2026-04-13：修复 `src/app/api/auth/[...all]/route.ts` 里动态同步 Better Auth 回调域名时直接写 `auth.options.baseURL/basePath` 导致的 TypeScript 阻塞，改成只更新运行时 `AuthContext.options/baseURL`；原因是 `betterAuth({...})` 返回的 `auth.options` 只保留项目显式传入配置类型，不含库补充的 `baseURL/basePath` 字段；已通过 `pnpm exec biome check --write src/app/api/auth/[...all]/route.ts` 与 `pnpm exec tsc --noEmit --pretty false` 验证，`pnpm test:run` 仍被仓库当前其他模块 13 个失败用例阻塞，包括 newsletter、payment webhook、storage、tool-config 与 redink 默认 provider 测试
- 2026-04-13：收口 AI 网关的 provider 协议适配，给模型绑定补 `endpointType/pollType/operations` 元数据并让 `lib/ai/openai.ts` 按声明选择 `chat/completions`、`images/generations`、`images/edits`、`videos/generations` 及对应轮询路径，同时给统一 AI route 与 RedInk 图片 route 增加显式 `operation/async/video` 输入并把旧的 `background=true` 仅保留为异步兼容别名；原因是之前大量行为依赖 `extraBody + 模型名` 特判，Geek 这类多端点中转站容易误路由、误轮询，且 phase3 多模态回归还被旧字段权限卡住；已通过 `pnpm exec biome check src/lib/ai/openai.ts src/lib/ai/index.ts src/features/ai-gateway/service.ts src/features/ai-gateway/admin.ts src/features/ai-gateway/components/admin-ai-gateway-view.tsx src/features/tool-config/service.ts src/app/api/platform/ai/chat/route.ts src/app/api/platform/redink/image/route.ts src/test/tool-config/ai-client.test.ts`、`pnpm test:run src/test/tool-config/ai-client.test.ts`、`pnpm test:run src/test/platform/redink-phase4-image.test.ts`、`pnpm test:run src/test/platform/redink-phase5-regression.test.ts`、`pnpm test:run src/test/platform/ai-chat-multimodal-phase3.test.ts` 验证，`pnpm exec tsc --noEmit --pretty false` 仍被仓库既有 `src/app/api/auth/[...all]/route.ts` 的 `baseURL/basePath` 类型错误阻塞
- 2026-04-12：集中修复 hydration mismatch 根因，收口所有 `useSession()` 驱动的首屏分支并给全局 `LanguageSwitcher` 增加挂载前稳定占位，覆盖 `admin/dashboard` 侧边栏、营销页 Header/Pricing/CTA，原因是服务端首屏和客户端首屏节点数量不一致会把后续 Radix `DropdownMenu`/`Tabs` 的 `radix-*` id 序列挤偏，表现为语言切换器和工具配置 Tabs 延迟报 hydration mismatch；已通过 `pnpm exec biome check src/features/shared/components/language-switcher.tsx src/features/admin/components/admin-sidebar.tsx src/features/dashboard/components/sidebar.tsx src/features/marketing/components/header.tsx src/features/marketing/components/pricing-section.tsx src/features/marketing/components/cta-section.tsx`、`pnpm exec tsc --noEmit --pretty false`（仍被仓库既有 `src/app/api/auth/[...all]/route.ts` 的 `baseURL/basePath` 类型错误阻塞）、`DETACH=1 SKIP_INSTALL=1 ./start-dev.sh`、`curl -I http://127.0.0.1:3000` 与 `curl -I https://platform.tripai.icu` 验证并完成服务重启
- 2026-04-12：修复通用 `Button` 在 `asChild` 模式下与 `next-intl` 的 `Link` 组合时生成嵌套 `<a>` 导致 hydration 报错，改成 `asChild` 分支直接克隆唯一子元素并合并按钮样式，同时保留 loading 内容替换与 `aria-disabled` 状态；已通过 `pnpm exec biome check src/components/ui/button.tsx`、`pnpm exec tsc --noEmit --pretty false`（仍被仓库既有 `src/app/api/auth/[...all]/route.ts` 的 `baseURL/basePath` 类型错误阻塞）、`DETACH=1 SKIP_INSTALL=1 ./start-dev.sh`、`curl -I http://127.0.0.1:3000` 验证并完成服务重启
- 2026-04-12：修复通用 `Button` 在 `asChild` 模式下叠加 loading 内容时向 Radix `Slot` 传入多个子节点，导致 dashboard 页面触发 `React.Children.only` 报错；改成 `asChild` 分支只透传单个 React 元素并保留内部内容替换；已通过 `pnpm exec biome check src/components/ui/button.tsx`、`pnpm exec tsc --noEmit --pretty false`（仍被仓库既有 `src/app/api/auth/[...all]/route.ts` 的 `baseURL/basePath` 类型错误阻塞）、`DETACH=1 SKIP_INSTALL=1 ./start-dev.sh`、`curl -I http://127.0.0.1:3000` 与 `curl -I https://platform.tripai.icu` 验证并完成服务重启
- 2026-04-11：去掉 RedInk 模型目录对 `json1` 白名单的二次过滤，改成只按 `json4` 目录配置与 AI 绑定能力返回文本/图片模型，原因是后台已把 `nano-banana` 配进 `json4.image_generation` 但前端仍为空，实查发现被 `json1` 挡掉；已通过真实登录 `redink.tripai.icu` 请求 `/api/platform/redink/model-options?projectKey=nextdevtpl` 复现空图片模型、数据库核对 `json4`/绑定状态，以及 `pnpm test:run src/test/platform/redink-model-options.test.ts src/test/platform/redink-phase5-regression.test.ts` 与 `pnpm exec biome check src/features/redink/service.ts src/test/platform/redink-model-options.test.ts` 验证
- 2026-04-11：为后台配置保存类按钮补统一 loading 反馈，给通用 `Button` 增加可选 `loading/loadingText`，并接入管理员工具配置、用户工具配置、AI Gateway 与存储配置保存按钮，原因是像 redink 配置保存这类操作之前只禁用按钮但没有明显提交态，用户难以判断是否点击成功；待通过 `pnpm exec biome check src/components/ui/button.tsx src/features/tool-config/components/admin-tool-config-view.tsx src/features/tool-config/components/user-tool-config-section.tsx src/features/ai-gateway/components/admin-ai-gateway-view.tsx src/features/storage/components/admin-storage-view.tsx` 验证
- 2026-04-11：继续修正 dashboard/admin 手机侧边栏高度计算，把移动端抽屉内容改成三段式 grid 布局并把抽屉高度从 `dvh` 调整为更稳定的 `svh`，同时补顶部和底部安全区内边距，原因是首轮修正后仍有手机浏览器把抽屉高度算偏，导致菜单区域起始位置不稳定；待通过 `pnpm exec biome check src/features/dashboard/components/sidebar.tsx src/features/admin/components/admin-sidebar.tsx` 验证
- 2026-04-11：修复 dashboard/admin 移动端侧边栏抽屉把菜单区域挤到底部的问题，给抽屉内容改成完整视口高度并让中间导航区单独滚动，同时为头部和底部用户区加 `shrink-0` 防止被压缩，原因是手机上展开菜单时首屏优先落在底部导致顶部菜单显示不全；已通过 `pnpm exec biome check src/features/dashboard/components/sidebar.tsx src/features/admin/components/admin-sidebar.tsx` 验证，`pnpm exec tsc --noEmit --pretty false` 仍被仓库既有 `src/app/api/auth/[...all]/route.ts` 的 `baseURL/basePath` 类型错误阻塞
- 2026-04-11：修复 `start-dev.sh` 会在空端口清理结果和非法 Python 局域网 IP 命令上被 `set -e` 提前退出的问题，并把后台驻留改成 `setsid` 且用 `ss` 补抓 Next 子进程监听 PID，原因是直接执行 `./start-dev.sh` 会立刻回到 shell 且 `3000` 无监听、旧 Next 进程也不会被清掉；已通过 `SKIP_INSTALL=1 START_TUNNEL=0 timeout 15s ./start-dev.sh`、端口占用场景下 `DETACH=1 SKIP_INSTALL=1 START_TUNNEL=0 ./start-dev.sh`、`ss -ltnp | rg ':3000\\b'` 与 `curl -I http://127.0.0.1:3000` 验证
- 2026-04-11：把 `/zh/admin/tool-config` 的 JSON 配置项默认展示切到格式化 JSON 视图，仍保留表单/JSON 切换，原因是后台当前希望优先看到可读性更高的 formatted JSON；已通过 `pnpm test:run src/test/tool-config/admin-tool-config-view.test.tsx` 与 `pnpm exec biome check src/features/tool-config/components/admin-tool-config-view.tsx src/test/tool-config/admin-tool-config-view.test.tsx` 验证
- 2026-04-11：为 dashboard/admin 布局补齐全局语言与主题切换入口，修复 `AdminSidebar` 未接入 `next-themes` 导致后台无法切换主题，并把支持中心与管理员首页的主要硬编码文案改成 `next-intl` 翻译键，同时给导航配置补 `titleKey` 便于后续新增菜单直接走国际化；已通过 `pnpm exec biome check --write ...`、`pnpm test:run src/test/support/ticket.test.ts src/test/support/admin-users.test.ts`、`pnpm test:run src/test/settings/profile.test.ts` 验证，`pnpm exec tsc --noEmit --pretty false` 仍被仓库既有 `src/app/api/auth/[...all]/route.ts` 的 `baseURL/basePath` 类型错误阻塞
- 2026-04-11：把 `/zh/admin/tool-config` 的 `json` 配置改成可直接逐项编辑的结构化表单，并保留格式化 JSON 视图切换，原因是后台当前只显示 JSON 字符串，编辑门槛高且容易出错；已通过 `pnpm test:run src/test/tool-config/admin-tool-config-view.test.tsx` 与 `pnpm exec biome check src/features/tool-config/components/admin-tool-config-view.tsx src/test/tool-config/admin-tool-config-view.test.tsx` 验证，`pnpm exec tsc --noEmit --pretty false` 仍被仓库既有 `src/app/api/auth/[...all]/route.ts` 的 `baseURL/basePath` 类型错误阻塞
- 2026-04-10：把 `geekai` provider 的数据库密钥替换成真实 Geek API Key，并复查 `redink` 当前模型目录与 Geek `/models` 返回结果一致，原因是前一轮清库后发现库里保存的是错误字符串而不是实际密钥；已通过真实请求 `GET https://geekai.co/api/v1/models`、`POST https://geekai.co/api/v1/chat/completions` 与 `psql` 查询验证 key 可用且 `redink` 配置的 9 个模型全部存在
- 2026-04-10：清理当前数据库里的 RedInk 与 Geek 配置，删除 `geek-default` mock provider、重建 `geekai` 的 9 个模型绑定、收敛 `redink` 管理员模型白名单/前端模型目录/计费规则，并清空 822 条 `redink` 测试用户配置，原因是库内同时混有 mock provider、旧 feature 名和大量测试残留导致实际配置难以判断；已通过 `psql` 实查 provider、binding、`tool_config_value`、`tool_config_field` 与 `ai_pricing_rule` 验证当前仅剩 `geekai` 一套配置且 `redink` 用户态脏数据已为 0，当前唯一阻塞为 `geekai` 的 API Key 字段仍是错误内容，需在后台重填真实密钥
- 2026-04-10：修复 `storage` 聚合出口把数据库与本地存储 provider 混入客户端构建的问题，并确认 `start-prod.sh` 现已可完成生产构建与启动，原因是设置页头像上传从聚合入口导入时把 `records` 和 `next/headers` 带进浏览器端，导致一键生产启动在 `next build` 阶段失败；已通过 `pnpm exec biome check src/features/storage/index.ts src/features/settings/components/settings-profile-view.tsx src/app/api/upload/presigned/route.ts src/app/api/jobs/storage/cleanup/route.ts src/app/api/platform/results/save/route.ts 'src/app/[locale]/(admin)/admin/storage/page.tsx' src/app/api/platform/storage/admin/cleanup-scope/route.ts src/app/api/platform/storage/admin/cleanup-expired/route.ts src/app/api/platform/storage/presigned-image/route.ts src/test/platform/storage-phase45-policy.test.ts`、`APP_NAME=NextjsTpl-prod-test2 PORT=3311 SKIP_INSTALL=1 START_TUNNEL=0 bash start-prod.sh` 与 `curl --max-time 20 -L -o /tmp/nextdevtpl-prod2-index.html -D - http://127.0.0.1:3311` 验证
- 2026-04-10：把 `start-prod.sh` 改成默认安装依赖、默认构建、等待本地服务就绪后再启 Cloudflare Tunnel 的生产一键启动脚本，并新增 `start-dev.sh` 作为调试一键启动入口，原因是原脚本默认跳过构建且不覆盖调试场景；已通过 `pnpm install --frozen-lockfile`、`bash -n start-prod.sh`、`bash -n start-dev.sh`、`PORT=3010 SKIP_INSTALL=1 START_TUNNEL=0 timeout 25s bash -x start-dev.sh` 验证调试链路，生产链路执行 `APP_NAME=NextjsTpl-prod-test PORT=3310 SKIP_INSTALL=1 START_TUNNEL=0 bash start-prod.sh` 时暴露仓库现有 `next build` 错误，阻塞点为 `src/features/storage/providers/local.ts` 与 `src/features/settings/components/settings-profile-view.tsx` 相关构建依赖
- 2026-04-10：完成 RedInk 模型目录 Phase 5，新增 `redink-phase5-regression` 完整回归链路测试并确认旧 AI Chat 测试未回退，原因是要在收尾前验证目录读取、文本生成、图片任务和结果轮询已经形成可直接使用的闭环；已通过 `pnpm test:run src/test/platform/redink-phase5-regression.test.ts src/test/platform/redink-model-options.test.ts src/test/platform/redink-phase2-proxy.test.ts src/test/platform/redink-phase3-text.test.ts src/test/platform/redink-phase4-image.test.ts src/test/platform/ai-chat.test.ts`、`pnpm exec tsc --noEmit --pretty false` 与 `pnpm exec biome check src/features/ai-gateway/service.ts src/features/redink/request-schema.ts src/features/redink/service.ts src/app/api/platform/redink/model-options/route.ts src/app/api/platform/redink/request-result/route.ts src/app/api/platform/redink/text/route.ts src/app/api/platform/redink/image/route.ts src/test/platform/redink-phase5-regression.test.ts` 验证
- 2026-04-10：完成 RedInk 模型目录 Phase 4，新增 `redink/image` 图片代理与共享消息 schema，原因是商品发布图和通用图片生成需要按管理员目录选模，并且任务型出图要复用 RedInk 自己的轮询接口；已通过 `pnpm test:run src/test/platform/redink-phase4-image.test.ts`、`pnpm exec tsc --noEmit --pretty false` 与 `pnpm exec biome check src/features/redink/request-schema.ts src/features/redink/service.ts src/app/api/platform/redink/image/route.ts src/app/api/platform/redink/request-result/route.ts src/app/api/platform/redink/text/route.ts src/test/platform/redink-phase4-image.test.ts` 验证
- 2026-04-10：完成 RedInk 模型目录 Phase 3，新增 `redink/text` 文本代理并让 AI 网关支持按 `projectKey` 解析工具配置，原因是标题、正文和商品发布文案需要按管理员目录选模且不能被默认项目配置覆盖；已通过 `pnpm test:run src/test/platform/redink-phase3-text.test.ts`、`pnpm exec tsc --noEmit --pretty false` 与 `pnpm exec biome check src/features/ai-gateway/service.ts src/features/redink/service.ts src/app/api/platform/redink/text/route.ts src/test/platform/redink-phase3-text.test.ts` 验证
- 2026-04-10：完成 RedInk 模型目录 Phase 2，新增 `redink/request-result` 代理接口并让 `model-options` 支持 `ETag` 与 `304`，原因是前端需要按 RedInk 命名空间轮询任务结果并缓存模型目录；已通过 `pnpm test:run src/test/platform/redink-phase2-proxy.test.ts`、`pnpm exec tsc --noEmit --pretty false` 与 `pnpm exec biome check src/features/redink/service.ts src/app/api/platform/redink/model-options/route.ts src/app/api/platform/redink/request-result/route.ts src/test/platform/redink-phase2-proxy.test.ts` 验证
- 2026-04-10：完成 RedInk 用户可见模型目录 Phase 1，新增管理员专用 `json4` 模型目录配置、用户态 `model-options` 接口和能力过滤测试，原因是需要让 RedInk 只向用户暴露管理员开放且已在 AI 网关启用的模型子集；已通过 `pnpm test:run src/test/platform/redink-model-options.test.ts`、`pnpm exec tsc --noEmit --pretty false` 与 `pnpm exec biome check src/features/tool-config/service.ts src/app/api/platform/redink/model-options/route.ts src/test/platform/redink-model-options.test.ts` 验证
- 2026-04-09：为 AI 模型绑定新增能力声明并在网关按能力筛选 provider，原因是图片生成、音视频输入输出不能再默认所有模型都支持；已通过 `pnpm test:run src/test/platform/ai-chat.test.ts src/test/platform/ai-chat-multimodal-phase1.test.ts src/test/platform/ai-chat-multimodal-phase2.test.ts src/test/platform/ai-chat-multimodal-phase3.test.ts src/test/platform/ai-admin-management.test.ts src/test/platform/ai-admin-ops.test.ts` 与 `pnpm exec tsc --noEmit --pretty false` 验证
- 2026-04-09：修复极客智坊图片任务轮询路径与状态映射，并为图片输出请求自动补 `image_generation`，原因是 `product-post-image` 创建后需通过 `/chat/{id}` 轮询且 `succeed` 需要视为完成，否则 `redink` 会在轮询阶段失败或只拿到文本说明；已通过 `pnpm test:run src/test/tool-config/ai-client.test.ts src/test/platform/ai-chat-multimodal-phase3.test.ts`、`pnpm exec tsc --noEmit --pretty false` 和真实上游 `/chat/{id}` 查询验证
- 2026-04-09：修复 `redink` 图片生成走 AI 网关时把异步 `pending` 任务误判为空响应的问题，并让后台失败请求回填实际尝试的 provider，原因是 `gemini-2.5-flash` 图片请求会先返回仅含任务元信息的 201 响应且管理台此前显示“未命中”容易误导；已通过 `pnpm test:run src/test/tool-config/ai-client.test.ts`、`pnpm test:run src/test/platform/ai-admin-management.test.ts` 和 `pnpm exec tsc --noEmit --pretty false` 验证
- 2026-04-09：把 AI 资源访问方式接入管理员工具配置，支持按工具切换 `public/proxy`，原因是需要在后台随时切换 OSS 直连和平台代理回源；已通过 `pnpm typecheck` 和 `pnpm exec biome check ...` 验证，`pnpm test:run src/test/platform/storage-phase1-provider.test.ts` 因缺少 `.env.test` 的 `DATABASE_URL` 未能执行
- 2026-04-09：新增管理员对象存储页面，展示运行时存储配置、按工具 AI 资源访问方式、资源记录明细和过期清理入口，原因是后台此前没有集中查看和管理存储能力；已通过 `pnpm typecheck` 与 `pnpm exec biome check ...` 验证
- 2026-04-09：补齐 `.env.test` 以对接本地 PostgreSQL `nextdevtpl`，并完成存储相关平台测试，原因是测试框架只读取 `.env.test`；已通过 `pnpm test:run src/test/platform/storage-*.test.ts`
- 2026-04-09：补齐 storage Phase 4/5 主功能，包括后台生命周期策略、前缀规则、按 requestId/taskId 主动清理和 cron 过期清理，原因是文档剩余功能尚未闭环；已通过 `pnpm typecheck` 与 `pnpm test:run src/test/platform/storage-*.test.ts`
- 2026-04-09：完成 `redink` 真实对象存储链路验收，并更新规划文档，原因是需要确认后台页面、上传、归档和按 requestId 清理在真实环境可用；已通过本地真实接口验收与 `pnpm test:run src/test/platform/storage-*.test.ts`
